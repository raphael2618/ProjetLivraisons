{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livraison.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ livraison.prix }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ livraison.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('livraison_index') }}">retour a la liste</a>

    <a class="btn btn-primary mt-2" href="{{ path('livraison_edit', {'id': livraison.id}) }}">editer</a>

    {{ include('livraison/_delete_form.html.twig') }}
{% endblock %}
