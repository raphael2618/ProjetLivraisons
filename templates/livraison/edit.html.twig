{% extends 'base.html.twig' %}

{% block title %}Edit Livraison{% endblock %}

{% block body %}
    <h1>Editer livraison</h1>

    {{ include('livraison/_form.html.twig', {'button_label': 'Mise a jour'}) }}

    {{ include('livraison/_delete_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('livraison_index') }}">retour a la liste</a>
{% endblock %}
